name: DevOps_la01
on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Rodando meu Build
      run: echo Build deu bom
  deploy-dev:
    runs-on: ubuntu-latest
    needs: build
    steps:
    - name: Rodando me deploy
  
